# 2026-02-22 Memory Log

## Morning
- Woke up and initialized identity.
- User (Yihao) named me "Dan".
- Discussed memory architecture: User prefers fast vector search over raw markdown reading.
- Attempted to install search tools (Brave, DuckDuckGo) but hit environment limits.
- **Decision**: Refactoring memory to use LanceDB locally with @xenova/transformers embeddings. This bypasses API requirements and ensures speed.

## Insights
- Yihao values efficiency and "smart" tools.
- Prefers direct answers over meta-talk.
- The environment is restricted (no pip, no sudo), so Node.js solutions are best.
